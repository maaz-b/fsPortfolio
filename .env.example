# EmailJS (https://www.emailjs.com/) — optional; without these, the contact form uses mailto.
# Copy to `.env.local` and restart `npm run dev`.
VITE_EMAILJS_PUBLIC_KEY=
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
